services:
  - type: web
    name: runner-2d-multi
    env: node
    plan: free
    region: frankfurt
    buildCommand: npm install
    startCommand: node server.js
    autoDeploy: true
    healthCheckPath: /